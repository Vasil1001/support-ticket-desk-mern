const mongoose = require('mongoose');

// * Each ticket will be connected to a user, 
// *a title, a description, a priority, a status, and a date
const ticketSchema = mongoose.Schema({
    user: {

    },
})